% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logging_helpers.R
\name{start_script_logging}
\alias{start_script_logging}
\title{Start script-level logging when available}
\usage{
start_script_logging(
  script_name = NULL,
  capture_prints = TRUE,
  capture_messages = TRUE,
  envir = parent.frame()
)
}
\arguments{
\item{script_name}{Optional label for the script in the log.
Defaults to the current sourced filename when available.}

\item{capture_prints}{Logical; if \code{TRUE} (default), capture printed output.}

\item{capture_messages}{Logical; if \code{TRUE} (default), also capture
messages and warnings when capturing prints.}

\item{envir}{Environment used to store the ownership flag.
Defaults to \code{parent.frame()}.}
}
\value{
Invisibly returns \code{TRUE} when this call started logging,
otherwise \code{FALSE}.
}
\description{
Starts script timing and print capture when the PICARD logger has been
configured. When the script already runs inside a managed PICARD context,
the function becomes a no-op so the same incipit can be used both
standalone and inside the pipeline.
}
