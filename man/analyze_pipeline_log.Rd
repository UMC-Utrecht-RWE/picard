% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post_run_analysis.R
\name{analyze_pipeline_log}
\alias{analyze_pipeline_log}
\title{Analyse a pipeline log and surface runtime bottlenecks}
\usage{
analyze_pipeline_log(
  log_file = NULL,
  log_dir = "logs",
  pattern = "^pipeline_.*\\\\.log$",
  output_dir = NULL,
  tz = "Europe/Amsterdam",
  top_n = 10,
  create_plots = TRUE,
  write_tables = TRUE
)
}
\arguments{
\item{log_file}{Optional path to a specific log file. If \code{NULL},
the most recent \code{pipeline_*.log} found in \code{log_dir} is used.}

\item{log_dir}{Directory containing pipeline logs. Default \code{"logs"}.}

\item{pattern}{Pattern used to discover pipeline logs.
Default \code{"^pipeline_.*\\\\\\\\.log$"}.}

\item{output_dir}{Optional output directory for plots and tables.
If \code{NULL}, an \verb{analysis/<log_name>} folder is created next to the log.}

\item{tz}{Time zone used to parse timestamps.
Default \code{"Europe/Amsterdam"}.}

\item{top_n}{Number of rows to keep in ranked summaries and bottleneck plots.
Default \code{10}.}

\item{create_plots}{Logical. If \code{TRUE}, save diagnostic plots.
Default \code{TRUE}.}

\item{write_tables}{Logical. If \code{TRUE}, save summary tables as CSV files.
Default \code{TRUE}.}
}
\value{
A named list with the selected log path, parsed entries,
file-level summary, and analysis tables.
}
\description{
This function reads a PICARD pipeline log, defaults to the latest
\code{pipeline_*.log} in \code{log_dir}, and returns a compact analysis bundle.
For \code{Normal} and \code{High} verbosity logs it estimates the slowest steps
and scripts; for \code{High} verbosity logs it also highlights the longest
silent gaps between log entries.

When \code{create_plots} or \code{write_tables} is \code{TRUE}, the function writes
outputs to an analysis folder next to the selected log.
}
